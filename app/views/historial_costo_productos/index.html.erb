<h1>Listing historial_costo_productos</h1>
<p></p>
<hr>

<%= link_to 'New Historial costo producto', new_historial_costo_producto_path, :class => "btn btn-primary float-right" %>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Precio</th>
      <th>Fecha</th>
      <th>Producto</th>
      <th></th>
      <!-- <th></th>
      <th></th> -->
    </tr>
  </thead>

  <tbody>
    <% @historial_costo_productos.each do |historial_costo_producto| %>
      <tr>
                    <td><%= historial_costo_producto.precio %></td>
                    <td><%= historial_costo_producto.fecha %></td>
                    <td><%= historial_costo_producto.producto.name %></td>
                <td><%= link_to 'Detail', historial_costo_producto %></td>
        <!-- <td><%= link_to 'Edit', edit_historial_costo_producto_path(historial_costo_producto) %></td>
        <td><%= link_to 'Destroy', historial_costo_producto, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
      </tr>
    <% end %>
  </tbody>
</table>

<br>
