<%= form_for(@tipo_usuario) do |f| %>
<% if @tipo_usuario.errors.any? %>
<div id="error_explanation">
<table style="background-color:#FF9595;border:solid 5px #d9534f;margin-left:150px">
<tr>
<td>
    <h2>Hubo <%= pluralize(@tipo_usuario.errors.count, "error") %> error(es) al agregar el nuevo tipo de usuario:</h2>

    <ul>
        <% @tipo_usuario.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
        <% end %>
    </ul>
</div>
</td>
</tr>
</table>
<% end %>

<div class="form-group">
                <%= f.label :nombre %>
            <%= f.text_field :nombre,maxlength:50, :class => "form-control" %>
    </div>
<div class="actions">
    <%= f.submit "Aceptar", :class => "btn btn-primary" %>
</div>
<% end %>
