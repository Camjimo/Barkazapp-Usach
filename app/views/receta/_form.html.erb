<%= form_for(@recetum) do |f| %>
<% if @recetum.errors.any? %>
<div id="error_explanation">
    <h2><%= pluralize(@recetum.errors.count, "error") %> prohibited this recetum from being saved:</h2>

    <ul>
        <% @recetum.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
        <% end %>
    </ul>
</div>
<% end %>

<div class="form-group">
                <%= f.label :cantidad_compuesto %>
            <%= f.number_field :cantidad_compuesto, :class => "form-control" %>
    </div>
<div class="form-group">
                <%= f.label :material_id %>
            <%= f.collection_select :material_id, Material.all, :id, :name, prompt: true, :class => 'form-control'  %>
    </div>
<div class="form-group">
                <%= f.label :producto_id %>
            <%= f.collection_select :producto_id, Producto.all, :id, :name, prompt: true, :class => 'form-control'  %>
    </div>
<div class="actions">
    <%= f.submit :class => "btn btn-success" %>
</div>
<% end %>
