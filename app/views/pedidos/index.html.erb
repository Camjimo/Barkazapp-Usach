<div>
<table width="100%">
<tr>
<td>
  
</td>
<td align="right">
<%= link_to 'Inicio',menu_index_path, :class => "btn btn-danger", :style => "color:white;" %>
<%= link_to 'Salir', destroy_user_session_path, :class => "btn btn-danger", :style => "color:white;" %> 
</td>
</tr>
</table>
<div style="background-color:white;min-height:492px;padding-top:16px;padding-bottom:91px;padding-left:200px;padding-right:200px;margin-top:3px">
  <h2 style="color:#660000"><strong>Comandas</strong></h2>
  <br/>
  <p>A contnuación se muestran las comandas registradas. Para agregar una nueva comanda, presione el botón "Nuevo" y para editarla seleccione "Ver Detalle".</p>
  <hr>

  <%= link_to 'Nuevo', new_pedido_path, :class => "btn btn-info float-left" %>

  <table class="table table-striped">
    <thead>
      <tr class="active">
        <th>Ver Detalle</th>
        <th>Hora</th>
        <th>Fecha</th>
        <th>Mesa</th>
        <th>Estado pedido</th>
        <th>Responsable</th>
        <!-- <th></th>
        <th></th> -->
      </tr>
    </thead>

    <tbody>
      <% @pedidos.each do |pedido| %>
        <tr>
                      <td><%= link_to 'Ver Detalle', pedido %></td>
                      <td><%= pedido.fecha.strftime('%X') %></td>
                      <td><%= pedido.fecha.strftime('%d') %>/<%= pedido.fecha.strftime('%m') %>/<%= pedido.fecha.strftime('%Y') %></td>
                      <td><%= pedido.mesa.id %></td>
                      <td><%= pedido.estado_pedido.nombre %></td>
                      <td><%= pedido.usuario.nombre %></td>
                  
          <!-- <td><%= link_to 'Edit', edit_pedido_path(pedido) %></td>
          <td><%= link_to 'Destroy', pedido, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
        </tr>
      <% end %>
    </tbody>
  </table>

  <br>
</div>
</div>