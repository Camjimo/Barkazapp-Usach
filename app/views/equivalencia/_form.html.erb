<%= form_for(@equivalencium) do |f| %>
<% if @equivalencium.errors.any? %>
<div id="error_explanation">
    <h2><%= pluralize(@equivalencium.errors.count, "error") %> prohibited this equivalencium from being saved:</h2>

    <ul>
        <% @equivalencium.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
        <% end %>
    </ul>
</div>
<% end %>

<div class="form-group">
                <%= f.label :cantidad %>
            <%= f.number_field :cantidad, :class => "form-control" %>
    </div>
<div class="form-group">
                <%= f.label :unidad_medida_id %>
            <%= f.collection_select :unidad_medida_id, UnidadMedida.all, :id, :name, prompt: true, :class => 'form-control'  %>
    </div>
<div class="form-group">
                <%= f.label :tipo_contenedor_id %>
            <%= f.collection_select :tipo_contenedor_id, TipoContenedor.all, :id, :name, prompt: true, :class => 'form-control'  %>
    </div>
<div class="actions">
    <%= f.submit :class => "btn btn-success" %>
</div>
<% end %>
