<h1>Listing detalle_pedidos</h1>
<p></p>
<hr>

<%= link_to 'New Detalle pedido', new_detalle_pedido_path, :class => "btn btn-primary float-right" %>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Cantidad venta</th>
      <th>Precio venta</th>
      <th>Pedido</th>
      <th>Producto</th>
      <th></th>
      <!-- <th></th>
      <th></th> -->
    </tr>
  </thead>

  <tbody>
    <% @detalle_pedidos.each do |detalle_pedido| %>
      <tr>
                    <td><%= detalle_pedido.cantidad_venta %></td>
                    <td><%= detalle_pedido.precio_venta %></td>
                    <td><%= detalle_pedido.pedido.name %></td>
                    <td><%= detalle_pedido.producto.name %></td>
                <td><%= link_to 'Detail', detalle_pedido %></td>
        <!-- <td><%= link_to 'Edit', edit_detalle_pedido_path(detalle_pedido) %></td>
        <td><%= link_to 'Destroy', detalle_pedido, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
      </tr>
    <% end %>
  </tbody>
</table>

<br>
