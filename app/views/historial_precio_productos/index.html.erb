<h1>Listing historial_precio_productos</h1>
<p></p>
<hr>

<%= link_to 'New Historial precio producto', new_historial_precio_producto_path, :class => "btn btn-primary float-right" %>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Precio</th>
      <th>Fecha</th>
      <th>Producto</th>
      <th></th>
      <!-- <th></th>
      <th></th> -->
    </tr>
  </thead>

  <tbody>
    <% @historial_precio_productos.each do |historial_precio_producto| %>
      <tr>
                    <td><%= historial_precio_producto.precio %></td>
                    <td><%= historial_precio_producto.fecha %></td>
                    <td><%= historial_precio_producto.producto.name %></td>
                <td><%= link_to 'Detail', historial_precio_producto %></td>
        <!-- <td><%= link_to 'Edit', edit_historial_precio_producto_path(historial_precio_producto) %></td>
        <td><%= link_to 'Destroy', historial_precio_producto, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
      </tr>
    <% end %>
  </tbody>
</table>

<br>
