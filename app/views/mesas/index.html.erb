<h1>Listing mesas</h1>
<p></p>
<hr>

<%= link_to 'New Mesa', new_mesa_path, :class => "btn btn-primary float-right" %>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Ocupada</th>
      <th></th>
      <!-- <th></th>
      <th></th> -->
    </tr>
  </thead>

  <tbody>
    <% @mesas.each do |mesa| %>
      <tr>
                    <td><%= mesa.ocupada %></td>
                <td><%= link_to 'Detail', mesa %></td>
        <!-- <td><%= link_to 'Edit', edit_mesa_path(mesa) %></td>
        <td><%= link_to 'Destroy', mesa, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
      </tr>
    <% end %>
  </tbody>
</table>

<br>
