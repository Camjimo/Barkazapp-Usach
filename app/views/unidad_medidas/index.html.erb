<h1>Listing unidad_medidas</h1>
<p></p>
<hr>

<%= link_to 'New Unidad medida', new_unidad_medida_path, :class => "btn btn-primary float-right" %>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Simbolo</th>
      <th></th>
      <!-- <th></th>
      <th></th> -->
    </tr>
  </thead>

  <tbody>
    <% @unidad_medidas.each do |unidad_medida| %>
      <tr>
                    <td><%= unidad_medida.nombre %></td>
                    <td><%= unidad_medida.simbolo %></td>
                <td><%= link_to 'Detail', unidad_medida %></td>
        <!-- <td><%= link_to 'Edit', edit_unidad_medida_path(unidad_medida) %></td>
        <td><%= link_to 'Destroy', unidad_medida, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
      </tr>
    <% end %>
  </tbody>
</table>

<br>
